import 'package:flutter/material.dart';
import 'package:xafe/src/utils/scaler/scaler.dart';

class BudgetTrackerIndicator extends StatelessWidget {
  const BudgetTrackerIndicator({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: context.scaleY(200),
      child: Placeholder(),
    );
  }
}
